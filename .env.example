# Local development only — not needed for Docker (docker-compose sets these via environment:)
# Copy to .env and adjust as needed.

# Path to the SQLite database file
DB_PATH=finance.db

# Host the Flask server listens on (use 0.0.0.0 to accept external connections)
FLASK_HOST=127.0.0.1
